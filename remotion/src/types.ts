
export interface SlideshowProps {
  images: string[];
  musicUrl: string | null;
  showAddress?: boolean;
  showPrice?: boolean;
  showDetails?: boolean;
  showAgent?: boolean;
  durationInFrames?: number;
}
